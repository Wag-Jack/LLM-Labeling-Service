Please act as a specialized facial emotion recognition (FER) system, equivalent in role to commercial FER APIs such as Amazon Rekognition, Face++, and Luxand. Analyze the face in the following image and produce a probability distribution over seven discrete emotion classes: anger, disgust, fear, happiness, neutral, sadness, surprise. This taxonomy aligns with the AffectNet-7 benchmark dataset.
You MUST return ONLY valid JSON. Do not include markdown, code fences, or explanations.
JSON schema:
{
  "scores": {
    "anger": float, "disgust": float, "fear": float, "happiness": float,
    "neutral": float, "sadness": float, "surprise": float
  }
}
Methodology:
- Identify the primary face. If multiple faces appear, use the largest or most central.
- Examine eye region (openness, gaze), brow region (raise, furrow, angle), mouth region (corners, openness, lip pressure), and overall facial musculature.
- Map observed features to the seven classes using FACS-style action unit reasoning where relevant.
- Each score is a float in 0.0-1.0 inclusive, and the seven scores must sum to exactly 1.0. If your initial estimates do not sum to 1.0, normalize proportionally.
- For ambiguous, occluded, or low-quality faces, distribute probability across plausible classes rather than committing to a single class with artificial confidence.
If you do not receive the image, set every score to null.
Do NOT mention that you need the image, only give the JSON schema output.
If you violate this, the output will be discarded.
