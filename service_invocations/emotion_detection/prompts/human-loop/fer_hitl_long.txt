Please act as part of a human-in-the-loop FER pipeline. N independent FER systems analyzed the same face image and produced probability distributions over seven classes: anger, disgust, fear, happiness, neutral, sadness, surprise. You do NOT have access to ground truth. Commit to your best verdict and report your confidence honestly; false confidence is worse than a low score. The pipeline routes low-confidence samples to a human reviewer, who provides the ground-truth label for those cases.
You MUST return ONLY valid JSON. Do not include markdown, code fences, or explanations.
JSON schema:
{
  "llm_emotion": string (your single best class from the seven listed),
  "winner": string (name of the most accurate system; must match a name below),
  "scores": {
    "<service_name>": float in 0.0-1.0 (per-service quality score, higher is better),
    ...one entry per system listed below...
  },
  "confidence": float in 0.0-1.0 (your confidence in "llm_emotion")
}
The "scores" object MUST contain exactly one entry per system name shown below (use the names verbatim). Include every system; do not invent extras.
Systems (name: output):
{service_blocks}
Decision protocol:
- Inspect the image; identify the dominant emotion and secondary cues.
- Compare each system's output against your visual reading and assign a per-service quality score in "scores".
- Set "winner" to the name of the highest-scoring system whose output best matches the visible face.
- Estimate your confidence in "llm_emotion" as a float in 0.0-1.0 and report it honestly.
- The pipeline automatically routes any sample with confidence below 0.85 to a human reviewer; you do not decide deferral yourself. Always record your best guess in "llm_emotion" and always name your best "winner".
- Lower your confidence when: the face is occluded or low-quality, the systems are near-equivalent, visible cues are mixed or contradictory, or the face appears to belong to a class outside the seven defined.
If you do not receive the image, set "llm_emotion" to null, "winner" to null, and every score in "scores" to null.
Do NOT mention that you need the image, only give the JSON schema output.
If you violate this, the output will be discarded.
