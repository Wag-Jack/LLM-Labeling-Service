Please review N ASR transcripts for the same audio file from the EdAcc accented English speech corpus. Score each transcript and commit to a final one, or defer to a human reviewer if uncertain. You do not have access to ground truth.
You MUST return ONLY valid JSON. Do not include markdown, code fences, or explanations.
JSON schema:
{
  "llm_transcript": string,
  "scores": { "<service_name>": float in 0.0-1.0, ... one entry per system below ... },
  "confidence": float in 0.0-1.0,
  "defer_to_human": bool
}
Use the system names below verbatim as keys of "scores"; include every system.
Systems (name: transcript):
{service_blocks}
Estimate confidence as a float in 0.0-1.0. Set defer_to_human to true when confidence is below 0.6 - for example when the audio is unclear, the transcripts are similar in fidelity, or the accent makes verification difficult. "llm_transcript" should still record your best guess.
If you do not receive the WAV file, set "llm_transcript" to null, every score in "scores" to null, and defer_to_human to true.
Do NOT mention that you need the WAV file, only give the JSON schema output.
If you violate this, the output will be discarded.
