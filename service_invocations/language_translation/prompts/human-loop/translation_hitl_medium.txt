Please review N French translations of the same English source from the EuroParl corpus. Score each one and commit to a final translation, or defer to a human reviewer if uncertain. You do not have access to a reference.
You MUST return ONLY valid JSON. Do not include markdown, code fences, or explanations.
JSON schema:
{
  "llm_translation": string,
  "scores": { "<service_name>": float in 0.0-1.0, ... one entry per system below ... },
  "confidence": float in 0.0-1.0,
  "defer_to_human": bool
}
Use the system names below verbatim as keys of "scores"; include every system.
English source: {source_text}
Systems (name: translation):
{service_blocks}
Estimate confidence as a float in 0.0-1.0. Set defer_to_human to true when confidence is below 0.6 - for example when the translations are similar in quality, the source is ambiguous, or specialized terminology makes verification difficult. "llm_translation" should still record your best guess.
If you do not receive the text, set "llm_translation" to null, every score in "scores" to null, and defer_to_human to true.
Do NOT mention that you need the text, only give the JSON schema output.
If you violate this, the output will be discarded.
