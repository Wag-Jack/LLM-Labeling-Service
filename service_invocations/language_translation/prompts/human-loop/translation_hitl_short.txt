N MT systems translated the same text into French. Please score each one and produce your own best translation, or defer to a human if uncertain. You do not have a reference.
You MUST return ONLY valid JSON. Do not include markdown, code fences, or explanations.
JSON schema:
{
  "llm_translation": string,
  "winner": string (name of the best system; must match a name below, or null if you defer),
  "scores": { "<service_name>": float in 0.0-1.0, ... one entry per system below ... },
  "confidence": float in 0.0-1.0,
  "defer_to_human": bool
}
Set defer_to_human to true if confidence < 0.6 (and set "winner" to null in that case). Use the system names below verbatim as keys; include every system.
English source: {source_text}
Systems (name: translation):
{service_blocks}
If you do not receive the text, set "llm_translation" to null, "winner" to null, every score in "scores" to null, and defer_to_human to true.
Do NOT mention that you need the text, only give the JSON schema output.
If you violate this, the output will be discarded.
